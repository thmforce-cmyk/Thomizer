# THOMIZER GUI Hardware v10

Focused UI correction for the upper control strip:

- Reworked THOMLAY AY-Y420 and FORCER into a cleaner analog hardware strip.
- Replaced bright bulky slider handles with compact black hardware-style faders with a thin light center line.
- Renamed the THOMLAY tail-control label from FORCER to GRIT so the master FORCER macro is not duplicated visually.
- Kept FORCER as the master macro with GENTLE -> NOT SO GENTLE endpoints.
- Strengthened safe-area side rails so note labels and grid controls sit inside the iPhone camera/love area.
- Kept the sequencer grid as the dominant visual element.

Upload the contents of this ZIP directly to the GitHub repository root.
