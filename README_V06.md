# THOMIZER v.alpha.0.6

Dense fullscreen layout pass. Removed visible LOCK/collapse controls, tightened all modules, reduced knob size, and set manifest display to fullscreen with standalone fallback.

Important: Safari browser chrome cannot be hidden in a normal browser tab. Add the site to Home Screen and launch it from the Thomizer icon for the no-browser PWA view.
