# THOMIZER GUI Hardware v12

Built from the uploaded v10 source package, not from the conflicting v11 branch.

Focused upper-strip correction:
- THOMLAY is wider, FORCER is narrower.
- THOMLAY / AY-Y420 is a real two-line device label.
- FORCER / TF-6000 is a real two-line device label.
- Delay power button and RATE select are equal-height controls and aligned to the same bottom line.
- MIX / FEEDBACK / GRIT each have their own label, range track, and value slot.
- GENTLE / NOT SO GENTLE uses the same light UI-text color family as select values.
- Top control strip, sequencer, and dock share the same content rail.
- REGEN and CLEAR are equal-width buttons in the dock action block.
- Range thumbs remain compact black hardware-style faders with a thin light center stripe.

Upload the contents of this ZIP directly to the GitHub repository root.
