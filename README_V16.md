# THOMIZER GUI Hardware v16

Top strip rebuilt into a consistent two-row control system.

Upload the ZIP contents directly into the GitHub repository root.

Changes:
- Upper row: control names.
- Lower row: actual controls.
- Module names are separated into compact left-side blocks.
- Slider values are now in boxed readouts beside their own sliders.
- DELAY button now shows ON/OFF because DELAY is already the label above.
- FORCER uses the same logic: title row, endpoint labels, slider, value box.
- Existing IDs and audio wiring preserved.

Version:
- v.alpha.0.16
