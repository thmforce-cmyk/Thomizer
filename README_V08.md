# THOMIZER GUI Hardware v08

Focused update only:

1. The sequencer grid is forced to fit all visible note rows inside the available panel height.
2. THOMLAY micro-knobs keep their values outside the knob and get a much larger touch target for easier control on mobile.

No other layout decisions were intentionally changed in this pass.
